# Neon Ledger backend

The backend is a Java 21 / Spring Boot 3.4 REST API for the Neon Ledger
educational banking simulation.

Run it with:

```bash
./mvnw spring-boot:run
```

Run all backend checks with:

```bash
./mvnw verify
```

See the repository-level README for architecture, deployment, API examples
and documented limitations.
